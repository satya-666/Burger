# Frontend
# Local default proxy target is http://localhost:5001/api.
BACKEND_API_URL=http://localhost:5001/api

# Vercel monorepo: set to /_/backend so the browser calls the backend service directly.
# NEXT_PUBLIC_API_BASE_URL=/_/backend
